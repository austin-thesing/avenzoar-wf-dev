{
  "name": "avenzoar-wf-dev",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "watch": "chokidar \"**/*.{ts,js,html,css}\" --ignore \"node_modules/**\" --ignore \"dist/**\" -c \"bun run build\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "html-minifier-terser": "^7.2.0",
    "csso": "^5.0.5",
    "terser": "^5.36.0"
  }
}

